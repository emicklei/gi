## genstdlib

this tool generates sources for the `pkg` to register all public functions,types,consts and variables from the current Go SDK version. The version must match that as defined in `go.mod` of the `gi` package.
